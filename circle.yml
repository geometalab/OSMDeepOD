machine:
  services:
    - docker

dependencies:
  override:
    - docker pull geometalab/osm-crosswalk-detection

test:
  override:
    - docker run -it --entrypoint="python" geometalab/osm-crosswalk-detection -m unittest discover tests
